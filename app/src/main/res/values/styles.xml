<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="CustomProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:progressTint">#FF0000</item> <!-- 设置进度条的颜色为红色 -->
        <item name="android:backgroundTint">#00FF00</item> <!-- 设置不确定模式下的颜色为绿色 -->
        <item name="android:layout_width">match_parent</item> <!-- 设置宽度为父布局宽度 -->
        <item name="android:layout_height">wrap_content</item> <!-- 高度为适应内容 -->
        <item name="android:orientation">horizontal</item> <!-- 设置进度条为水平 -->
    </style>
    <style name="Widget.ProgressBar.Horizontal" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/progress_horizontal</item>
        <item name="android:indeterminateDrawable">@drawable/progress_indeterminate_horizontal</item>
        <item name="minHeight">20dip</item>
        <item name="maxHeight">20dip</item>
        <item name="android:mirrorForRtl">true</item>
    </style>
</resources>